# Copy to docker/.env (gitignored). Compose interpolates these.
# Any long random string: openssl rand -hex 32
SEARXNG_SECRET=
# Postgres password for the kyraan database (localhost-only container).
# Any long random string: openssl rand -hex 24
KYRAAN_PG_PASSWORD=
