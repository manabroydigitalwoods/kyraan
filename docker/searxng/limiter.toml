# Intentionally empty — the limiter is off (settings.yml: server.limiter
# false; Kyraan on localhost is the sole client). The file exists only so
# botdetection stops logging "missing config file" on every startup.
